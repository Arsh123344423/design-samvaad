# ── Source artwork ──────────────────────────────────────────────
# The site loads optimised derivatives, which ARE committed. These
# originals total ~93 MB — the truck alone is 49 MB — so they stay
# local. See README → Assets for the commands that regenerate the
# derivatives from them.
Assets/Truck hero Image.png
Assets/Sakhi Talks.jpg
Assets/Navbar Logo.png
Assets/DS Image*.png
Speakers/DST*.png
Gallery/*.png
Gallery/IMG_*
Gallery/_DSC*
Gallery/image*

# ── Editor / OS ─────────────────────────────────────────────────
.DS_Store
.claude/
